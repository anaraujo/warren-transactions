& h3, & h6 {
    @apply font-bold border-b-px border-lighter-gray;
}

& h3 {
    @apply text-lg;
    letter-spacing: -1px;

    @screen sm-down {
        @apply text-base;
    }
}

& h6 {
    @apply text-xs text-gray font-serif pb-2;
}